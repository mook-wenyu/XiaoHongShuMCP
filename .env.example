# 环境变量示例（UTF-8 无 BOM）
# RoxyBrowser 本地 API：可省略，默认 http://127.0.0.1:50000 ，鉴权使用 Header `token`
# ROXY_API_HOST=127.0.0.1
# ROXY_API_PORT=50000
# 也可直接提供完整地址（优先级更高，例如 http://192.168.1.10:50000）
# ROXY_API_BASEURL=

# Roxy 访问令牌（必填）
ROXY_API_TOKEN=db3b0ed6f71e0eef03279bca449e7f8c

# 并发度与超时（毫秒）
MAX_CONCURRENCY=2
TIMEOUT_MS=60000

# 策略（限速/熔断）参数
POLICY_QPS=5
POLICY_FAILURES=5
POLICY_OPEN_SECONDS=15

# 策略档位参考（按需选择，一行一个示例，非变量，仅注释）
# 低风险：POLICY_QPS=2 POLICY_FAILURES=3 POLICY_OPEN_SECONDS=30
# 平衡型：POLICY_QPS=5 POLICY_FAILURES=5 POLICY_OPEN_SECONDS=15
# 激进型（不推荐）：POLICY_QPS=10 POLICY_FAILURES=8 POLICY_OPEN_SECONDS=8

# 任务默认访问 URL（可被 CLI 覆盖）
DEFAULT_URL=https://www.xiaohongshu.com/explore

# CLI 账号目录 ID 列表（兼容 --dir-ids 与 --dirId 多次传参）
# 示例：ROXY_DIR_IDS=accountA,accountB,accountC
# ROXY_DIR_IDS=

# 默认工作空间 ID（未显式传入 workspaceId 时使用）
ROXY_DEFAULT_WORKSPACE_ID=28255

# 日志（开发期可开启美化输出）
LOG_LEVEL=info
# LOG_PRETTY=true

# —— XHS 导航与滚动调优 ——
# xhs_select_note 每步滚动后截图（视口截图），默认开启；关闭请设为 false/0
XHS_SCROLL_DEBUG_SHOT=true
# 动态步长比例（视口高度×比例，范围 0.3–0.9），默认 0.55；同时受 XHS_CONF.scroll.step 作为上限约束
XHS_SCROLL_STEP_RATIO=0.55
# xhs_select_note 最大滚动轮次（默认 18）
XHS_SELECT_MAX_SCROLLS=18
